import {StyleSheet} from 'react-native'

const styles = StyleSheet.create({
    favButton: {
        resizeMode: 'contain',
        width: 45,
        height: 45,
        alignSelf: 'center'
    }
})

export default styles